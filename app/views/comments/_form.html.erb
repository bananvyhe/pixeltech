<%= form_with model: [commentable, Comment.new], local: true do |form| %>
	<div>
		<%= form.text_area :body, placeholder: "добавить комментарий", class: "form-control" %>
	</div>
	<div>
		<%= form.submit class: "button" %>
	</div>
<% end %>