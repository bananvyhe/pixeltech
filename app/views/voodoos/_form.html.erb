<%= form_with(model: voodoo, local: true) do |form| %>
  <% if voodoo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voodoo.errors.count, "error") %> prohibited this voodoo from being saved:</h2>

      <ul>
      <% voodoo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
