<!-- <p id="notice"><%= notice %></p> -->
<div class="lostcenter">
	<div class="mainframepaddings">
	  <h3><%= @post.title %></h3>
		<div class="mediumtext">
	  	<%=simple_format  @post.body %>
	  </div>
		<div style="padding-bottom: 0.4em">
		<%#= link_to 'редактировать', edit_post_path(@post) %> <!--  | -->
		<%= link_to 'назад', '/' %>		
		</div>
		<hr>
		<h4>комментарии:</h4>
		<div id="comments" class="mediumtext"> 
			<%= render @post.comments.where(parent_id: nil), max_nesting: 24 %> 
			<%= render partial: "comments/form", locals: {commentable: @post} %>
		</div>
	</div>
</div>